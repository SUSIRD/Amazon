<h1>Blog#index</h1>
<p>Find me in app/views/blog/index.html.erb</p>


<%= Post.only_published.each do |post| %>
  <%# if post.published == true %>
    <table>
      <tr><%= post.title %></tr>
      <tr><%= post.content %></tr>
    </table>
  <%# end %>
<% end %>

# Posts mas populares
<%= Post.popular.each do |post| %>
  <tr><%= post.title %></tr>
  <tr><%= post.content %></tr>
<% end %>
